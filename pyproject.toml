[project]
name = "agent-tst"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    # "google-generativeai",     # <-- REMOVED
    "python-dotenv",
    # "google-cloud-aiplatform", # <-- REMOVED
    "google-api-python-client", 
    "groq",
    "openai", # <-- Required for Cerebras's OpenAI-compatible API
]

[tool.setuptools]
packages = ["functions", "calculator"]